import { StorageInfoDataType } from '../types/inputData';
import { ResourceStorage } from './storage';
export declare abstract class StorageCreator {
    abstract getInstance(): ResourceStorage;
    getDataFromStorage(keys: Array<string>): Promise<{
        [key: string]: any;
    }>;
    putDataToStorage(data: {
        [key: string]: any;
    }): Promise<{
        errorKeys: Array<string>;
    }>;
    removeDataFromStorage(keys: Array<string>): Promise<{
        errorKeys: Array<string>;
    }>;
    clearStorage(): Promise<void>;
    getStorageInfo(): Promise<StorageInfoDataType>;
}
export declare class LocalStorageCreator extends StorageCreator {
    #private;
    constructor(appId: string);
    getInstance(): ResourceStorage;
}
export declare class NativeStorageCreator extends StorageCreator {
    #private;
    constructor(appId: string);
    getInstance(): ResourceStorage;
}
