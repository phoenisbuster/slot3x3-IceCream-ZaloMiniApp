import * as DataType from '../types/inputData';
import ZaloMiniProgramSDK from './zalo-mp-service';
export declare class Background {
    /**
     * Dynamically sets the background color of the window.
     *
     * @async
     * @function setBackgroundColor
     * @category Background
     *
     * @example
     * zmpApis.setBackgroundColor({
     *  backgroundColor: "#ffffff"
     * }).then((res) => {
     *  console.log("Success");
     * }).catch((error) => {
     *  console.log("Fail");
     * });
     *
     * @param {Object} object
     * @param {string} object.backgroundColor - The background color of the window, which must be a hexadecimal value
     *
     *
     */
    static setBackgroundColor(this: ZaloMiniProgramSDK, object: DataType.BackgroundColorType): Promise<any>;
}
