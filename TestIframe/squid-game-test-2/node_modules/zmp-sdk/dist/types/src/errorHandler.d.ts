import { ACTION } from './constants';
export declare class AppError {
    code: number;
    message: string;
    api?: string | symbol;
    isNativeError?: boolean;
    action?: typeof ACTION[keyof typeof ACTION];
    constructor(code: number, msg: string, api?: string | symbol);
    toString(): string;
    toJSON(): object;
}
export declare const errorHandler: (error: any, api?: string | symbol | undefined) => object;
