import { StorageInfoDataType } from '../types/inputData';
import { ResourceStorage } from './storage';
export declare class LocalResourceStorage implements ResourceStorage {
    #private;
    constructor(prefix?: string);
    findAllCurrentDataByApp(prefix?: string): void;
    get(keys: Array<string>): Promise<Map<string, string | null>>;
    getSync(key: string): string | null;
    set(data: {
        [key: string]: string;
    }): Promise<{
        errorKeys: Array<string>;
    }>;
    setSync(key: string, value: any): void;
    getInfo(): Promise<any>;
    getInfoSync(): StorageInfoDataType;
    remove(keys: Array<string>): Promise<{
        errorKeys: Array<string>;
    }>;
    removeSync(key: string): void;
    clear(): Promise<void>;
    clearSync(): void;
}
