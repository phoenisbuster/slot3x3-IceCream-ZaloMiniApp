import { ResourceStorage } from './storage';
export declare class NativeResourceStorage implements ResourceStorage {
    #private;
    constructor(appId: string);
    get(keys: Array<string>): Promise<Map<string, string | null>>;
    set(data: {
        [key: string]: string;
    }): Promise<{
        errorKeys: Array<string>;
    }>;
    getInfo(): Promise<any>;
    remove(keys: Array<string>): Promise<{
        errorKeys: Array<string>;
    }>;
    clear(): Promise<any>;
}
